apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "multi-stage-react-app.fullname" . }}-test-connection"
  labels:
    {{- include "multi-stage-react-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "multi-stage-react-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
