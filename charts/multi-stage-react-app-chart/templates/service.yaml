apiVersion: v1
kind: Service
metadata:
  name: {{ include "multi-stage-react-app.fullname" . }}
  labels:
    {{- include "multi-stage-react-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multi-stage-react-app.selectorLabels" . | nindent 4 }}
